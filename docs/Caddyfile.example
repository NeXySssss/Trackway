s2-lt.nexy.one {
	encode zstd gzip

	# If trackway runs on the same host.
	# Replace 8083 with your actual dashboard.listen_address port.
	reverse_proxy 127.0.0.1:8083 {
		health_uri /healthz
		health_interval 10s
		health_timeout 2s
	}
}
